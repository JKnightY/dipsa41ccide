name: Golong web example
on:
  push:
    branches:
    - 'v1.0'
    tags:
    - 'latest'

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Create tag
        uses: rickstaa/action-create-tag@v1
        id: "tag_create"
        with:
          tag: "latest2"
          force_push_tag: true
          message: "Lastest release"
      - name: Release
        uses: ncipollo/release-action@v1
        with:
          skipIfReleaseExists: true
          tag: "latest2"
          artifacts: "release.tar.gz,foo/*.txt"
          bodyFile: "body.md"

      - name: Install Railway
        run: npm i -g @railway/cli

      - name: Deploy
        run: railway up
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN}}

      - name: Send custom JSON data to Slack workflow
        id: slack
        uses: slackapi/slack-github-action@v1.24.0
        with:
            payload: |
               {
                      "text": "WorkShop2 -- CICD -- JinKeyi",
                      "blocks": [
                      {
                        "type": "section", 
                        "text": {
                        "type": "mrkdwn",
                        "text": "Name : JinKeyi"
                        }
                      },
                      {
                        "type": "section",
                        "text": {
                        "type": "mrkdwn",
                        "text": "Matriculation : A0276819L" 
                        }
                      },
                      {
                        "type": "section", 
                        "text": {
                        "type": "mrkdwn",
                        "text": "Email : E1133134@u.nus.edu"
                        }
                      },
                      {
                        "type": "section",
                        "fields": [
                        {
                          "type": "mrkdwn",
                          "text": "Repository : https://github.com/JKnightY/dipsa41ccide"
                        }
                        ]
                      },
                      {
                        "type": "section",
                        "fields": [
                        {
                          "type": "mrkdwn",  
                          "text": "Deployment URL : https://dipsa41ccicd-production-59e9.up.railway.app"
                        }
                        ]
                      }
                      ]
                    }


        env:
            SLACK_WEBHOOK_URL : ${{ secrets.SLACK_WEBHOOK_URL }}
            SLACK_WEBHOOK_TYPE : INCOMING_WEBHOOK
